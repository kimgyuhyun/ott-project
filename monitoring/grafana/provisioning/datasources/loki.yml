# Grafana Loki 데이터소스 자동 프로비저닝 (로그)
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: ottloki
    access: proxy
    url: http://loki:3100   # monitoring 네트워크의 컨테이너 이름으로 도달
    editable: false
