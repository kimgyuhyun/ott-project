###############################
# Backend (Java/Gradle) .dockerignore
###############################

# VCS
.git
.gitignore

# Gradle caches and build outputs
.gradle
build
target
*.log

# Docker-related (not needed in build context)
Dockerfile
docker-compose*.yml
.dockerignore

# IDE/OS clutter
.idea
.vscode
*.iml
.DS_Store
Thumbs.db

# Runtime pid files (defensive)
pids/
*.pid
*.seed
*.pid.lock

# Keep Gradle wrapper JAR if copied from repo root; not needed here
!gradle/wrapper/gradle-wrapper.jar