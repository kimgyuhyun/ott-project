# 개발용 Dockerfile - 빠른 빌드와 핫 리로드
FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사 및 의존성 설치 (캐싱 최적화)
COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund

# 소스 코드는 볼륨 마운트로 처리 (COPY 생략)

# 개발 서버 실행
EXPOSE 3000
CMD ["npm", "run", "dev"]
